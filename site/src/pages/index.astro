---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Home">
  <section style="max-width: 640px;">
    <p style="font-family: var(--font-sans); font-size: 13px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--color-text-muted); margin: 0 0 var(--space-2);">
      Placeholder copy — structure only, not final voice
    </p>
    <h1 style="font-size: 44px; line-height: 1.2; font-weight: 400; margin: 0 0 var(--space-3);">
      A quieter way to change.
    </h1>
    <p style="font-family: var(--font-sans); font-size: 17px; line-height: 1.6; color: var(--color-text-muted); margin: 0 0 var(--space-4);">
      [TODO: hero paragraph — the "quiet, not loud" thesis, in Sophie's voice, localized together, not machine-translated. See /brand/POSITIONING-DRAFT.md.]
    </p>
    <a href="/program/" style="display: inline-block; font-family: var(--font-sans); font-size: 15px; padding: var(--space-2) var(--space-3); background: var(--color-accent); color: var(--color-bg); text-decoration: none; border-radius: var(--radius);">
      See the program
    </a>
  </section>
</Layout>
